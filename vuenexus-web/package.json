{
  "name": "vuenexus-web",
  "version": "0.1.0",
  "private": true,
  "description": "Browser graph explorer for VueNexus serve APIs.",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/build.mjs",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.17.0"
  }
}
